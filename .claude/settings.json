{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *.github/workflows/*.yml|*.github/workflows/*.yaml) actionlint \"$f\" 2>/dev/null || true ;; esac; }",
            "timeout": 15000
          }
        ]
      }
    ]
  }
}
